{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "preact": "^10.12.1",
    "typescript": "^4.9.5"
  },
  "devDependencies": {
    "esbuild-linux-64": "^0.15.18"
  },
  "scripts": {
    "build": "npm run build:story-viewer",
    "build:story-viewer": "./node_modules/esbuild-linux-64/bin/esbuild stories/main.tsx --bundle --outfile=dist/stories.js --minify"
  }
}
